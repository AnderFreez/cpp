0) подробно разобрать написанное на занятии
1) написать тесты по аналогии с существующими(я их там немного усовершенствовал) для всех фунций из myMath.h, 
кроме degree20(int x); degree23(int x); max(int* a, int size); min(int* a, int size);
